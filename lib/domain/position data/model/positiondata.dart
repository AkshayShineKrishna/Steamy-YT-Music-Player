class PositionData {
  const PositionData(
    this.position,
    this.bufferPosition,
    this.duration,
  );

  final Duration position;
  final Duration bufferPosition;
  final Duration duration;
}
